import { OnSaleStatePipe } from './on-sale-state.pipe';

describe('OnSaleStatePipe', () => {
  it('create an instance', () => {
    const pipe = new OnSaleStatePipe();
    expect(pipe).toBeTruthy();
  });
});
